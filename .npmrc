# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Ensure optional dependencies are installed (needed for Rollup, etc.)
# This is important for native bindings like @rollup/rollup-linux-x64-gnu
optional=true
