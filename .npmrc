# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Note: We don't disable optional dependencies here as Astro may need them
# Instead, we handle the oxc-parser issue in the Jenkinsfile by cleaning before install
